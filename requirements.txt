# Core dependencies. Install torch FIRST, matching your platform:
#   GPU (CUDA 12.4):  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   CPU only:         pip install torch --index-url https://download.pytorch.org/whl/cpu
# then:               pip install -r requirements.txt
# (no index-url here on purpose, so this file works for both installs)

transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.25.0
safetensors>=0.4.0
huggingface_hub>=0.19.0
numpy>=1.24.0
scipy>=1.10.0          # TDA: cKDTree, linear_sum_assignment (tda_analysis)
gudhi>=3.7.0           # TDA: SimplexTree persistence cross-check + H_1 (optional but recommended)
scikit-learn>=1.3.0
tqdm>=4.66.0
matplotlib>=3.7.0
pandas>=2.0.0

# GPTQ/AWQ baselines (v5/run_gptq_baseline.py). Optional — the script skips
# missing libraries gracefully:
# gptqmodel>=2.5.0
# autoawq>=0.2.0
